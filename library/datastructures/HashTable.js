// Generated by CoffeeScript 1.3.3
(function() {
  var HashTable;

  module.exports = HashTable = (function() {

    function HashTable() {}

    HashTable.prototype.table = {};

    HashTable.prototype.size = 0;

    HashTable.prototype.get = function(hash) {
      return this.table[hash];
    };

    HashTable.prototype.set = function(hash, value) {
      if (!this.contains(hash)) {
        ++this.size;
      }
      this.table[hash] = value;
      return this;
    };

    HashTable.prototype.remove = function(hash) {
      if (this.contains(hash)) {
        delete this.table[hash];
        --this.size;
      }
      return this;
    };

    HashTable.prototype.contains = function(hash) {
      return this.table[hash] != null;
    };

    HashTable.prototype.size = function() {
      return this.size;
    };

    HashTable.prototype.purge = function() {
      delete this.table;
      delete this.size;
      this.table = {};
      this.size = 0;
      return this;
    };

    return HashTable;

  })();

}).call(this);
